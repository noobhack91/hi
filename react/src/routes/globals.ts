// TypeScript configuration for React components

// Defining global variables for the React project
declare global {
  var Bahmni: boolean;
  var CryptoJS: boolean;
  var EncounterConfig: boolean;
  var FastClick: boolean;
  var Modernizr: boolean;
  var _: boolean;
  var angular: boolean;
  var c3: boolean;
  var d3: boolean;
  var defaults: boolean;
  var describe: boolean;
  var expect: boolean;
  var it: boolean;
  var keyboardJS: boolean;
  var lf: boolean;
  var moment: boolean;
  var purl: boolean;
  var window: boolean;
}

// Exporting an empty object to ensure the file is treated as a module
export {};
