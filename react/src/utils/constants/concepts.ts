// TypeScript constant for React components

// Define the type for the concepts constant
type Concepts = {
    age: string;
    weight: string;
};

// Define the concepts constant
const concepts: Concepts = {
    age: "Age",
    weight: "Weight"
};

export default concepts;
